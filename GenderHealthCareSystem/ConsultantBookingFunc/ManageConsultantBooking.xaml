<Window x:Class="GenderHealthCareSystem.ConsultantBookingFunc.ManageConsultantBooking"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GenderHealthCareSystem.ConsultantBookingFunc"
        mc:Ignorable="d"
        Title="Manage Consultant Booking" 
        Height="800"
        Width="1200"
        FontFamily="Arial"
        FontSize="13"
        Background="#FFD0E8D0"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="Tahoma"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#FF2E7D32"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="16"/>
            <Setter Property="Background" Value="#FFECF9EC"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#FF388E3C"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <StackPanel>
        <DataGrid x:Name="dgConsultantBookingList" AutoGenerateColumns="False" CanUserAddRows="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Booking Id" Width="0.7*" Binding="{Binding BookingId}"/>
                <DataGridTextColumn Header="Customer Name" Width="1*" Binding="{Binding Customer.FullName}"/>
                <DataGridTextColumn Header="Consultant Name" Width="1*" Binding="{Binding Consultant.FullName}"/>
                <DataGridTextColumn Header="Booking Date" Width="1.2*" Binding="{Binding BookingDate}"/>
                <DataGridTextColumn Header="Status" Width="1*" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="Payment Status" Width="1*" Binding="{Binding PaymentStatus}"/>
                <DataGridTextColumn Header="Meet Link" Width="1.5*" Binding="{Binding MeetLink}"/>
                <DataGridTextColumn Header="Notes" Width="1.2*" Binding="{Binding Note}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBox x:Name="txtSearch" Width="500"/>
            <Button x:Name="SearchBtn" Content="Search" Click="SearchBtn_Click"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
            <Button x:Name="btnStatus" Content="Update Status" Click="btnStatus_Click"/>
            <Button x:Name="btnGenerateLink" Content="Add Meeting Link" Click="btnGenerateLink_Click"/>
        </StackPanel>
        
        <Popup Placement="Center" IsOpen="False" x:Name="ppStatus">
            <StackPanel Orientation="Vertical" Margin="1" Background="White" Width="300" Height="250">
                <Label Content="Update Booking Status" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                <ComboBox x:Name="cbBookingStatus" Width="200">
                    <ComboBoxItem Content="Pending"/>
                    <ComboBoxItem Content="Confirmed"/>
                    <ComboBoxItem Content="Completed"/>
                    <ComboBoxItem Content="Cancelled"/>
                </ComboBox>
                <Label Content="Payment Status:" Margin="0,10,0,5"/>
                <ComboBox x:Name="cbPaymentStatus" Width="200">
                    <ComboBoxItem Content="Unpaid"/>
                    <ComboBoxItem Content="Paid"/>
                    <ComboBoxItem Content="Refunded"/>
                </ComboBox>
                <Button x:Name="btnUpdateStatus" Content="Update Status" Click="btnUpdateStatus_Click" Margin="0,10,0,0"/>
                <Button x:Name="btnClosePopup" Content="Close" Click="btnClosePopup_Click" Margin="0,10,0,0"/>
            </StackPanel>
        </Popup>

    </StackPanel>
</Window>