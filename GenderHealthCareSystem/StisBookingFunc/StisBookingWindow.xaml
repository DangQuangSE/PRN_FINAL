<Window x:Class="GenderHealthCareSystem.StisBookingFunc.StisBookingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GenderHealthCareSystem.StisBookingFunc"
        mc:Ignorable="d"
        Title="Danh sách đặt lịch STIs" 
        Height="500"
        Width="800"
        FontFamily="Arial"
        FontSize="13"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#0099CF"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <DockPanel>

        <!-- Nút quay lại -->
        <Button x:Name="btnBack" Content="← Quay lại" 
                DockPanel.Dock="Top"
                Margin="10"
                HorizontalAlignment="Left" Click="btnBack_Click"/>

        <StackPanel>
            <DataGrid x:Name="dgStisBookingList" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã đặt lịch" Width="0.7*" Binding="{Binding BookingId}"/>
                    <DataGridTextColumn Header="Tên dịch vụ" Width="1*" Binding="{Binding Service.ServiceName}"/>
                    <DataGridTextColumn Header="Giá" Width="1*" Binding="{Binding Service.Price}"/>
                    <DataGridTextColumn Header="Ngày đặt" Width="1*" Binding="{Binding BookingDate}"/>
                    <DataGridTextColumn Header="Trạng thái" Width="1*" Binding="{Binding Status}"/>
                    <DataGridTextColumn Header="Ghi chú" Width="1*" Binding="{Binding Note}"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnAdd" Content="Thêm lịch mới" Click="Add_Click"/>
                <Button x:Name="btnEdit" Content="Đổi ngày hẹn" Click="btnEdit_Click"/>
                <Button x:Name="btnDelete" Content="Hủy lịch" Click="Delete_Click"/>
                <Button x:Name="btnViewResult" Content="Xem kết quả" Click="btnViewResult_Click"/>
            </StackPanel>

            <Popup Placement="Center" x:Name="ppresult" StaysOpen="False" AllowsTransparency="True" PopupAnimation="Fade">
                <StackPanel Height="500" Width="450" Background="White" Margin="20" 
                            VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBox x:Name="tbresult" Width="400" Height="400" TextWrapping="Wrap"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             VerticalScrollBarVisibility="Auto"/>
                    <Button Content="Đóng" Click="btnClosePopup_Click" Margin="0,10,0,0"/>
                </StackPanel>
            </Popup>
        </StackPanel>
    </DockPanel>
</Window>
